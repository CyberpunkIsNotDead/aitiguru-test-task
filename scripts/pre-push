#!/bin/sh

# Pre-push hook that runs additional checks
# Exit on any error
set -e

echo "🚀 Running pre-push checks..."

# Run TypeScript compilation check
echo "Checking TypeScript compilation..."
npm run build
if [ $? -ne 0 ]; then
    echo "❌ TypeScript compilation failed. Fix the issues before pushing."
    exit 1
fi

echo "✅ All pre-push checks passed!"
exit 0
